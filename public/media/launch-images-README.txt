把设计好的两张"记下亮的 / 记下暗的"入口按钮图丢进本目录，文件名必须一致：

  launch-bright.png  → "记下亮的" 按钮底图
  launch-dark.png    → "记下暗的" 按钮底图

建议横图，比例约 16:9 ~ 2:1（按钮本身是矮宽的圆角矩形，高度 96px 左右），
宽边 ≥ 720px，主体别太满，底部留一点暗部/空间，因为下方会盖一层黑色渐变，
配合按钮上的文字图标（“记下亮的”/“记下暗的”）阅读。

没放图之前，按钮会显示一个暖色/冷色的渐变底作为占位，不会显得空。
